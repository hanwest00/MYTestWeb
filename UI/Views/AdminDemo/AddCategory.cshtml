@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>AddCategory</title>
    <script src="@Url.Content("../../Data/Scripts/jquery-1.5.1.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("../../Data/Scripts/jquery.form.mini.js")" type="text/javascript"></script>
    <script src="@Url.Content("../../Data/Scripts/jqueryExtends.js")" type="text/javascript"></script>
    <style>
        #per {
            background:#ccc;
            width:0px;
            text-align:center;
            line-height:30px;
        }
    </style>
</head>
<body>
    <div>
        @* @using (Html.BeginForm("AddCategory", "AdminDemo", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            <span>类名 : </span>@Html.TextBox("cateName")<br />
            <span>描述 : </span>@Html.TextBox("description")<br />
            <span>图片 : </span>@Html.TextBox("image", null, new { id = "upload_img", type = "file" })<br />
            @Html.TextBox("submit", "submit", new { type = "submit" })  @Html.TextBox("reset", "reset", new { type = "reset" })
        }*@ <span>图片 : </span>@Html.TextBox("image", null, new { id = "upload_img", type = "file" })<br />
        <div id="per">
        </div>
    </div>
</body>
</html>
<script type="text/javascript">
    $(document).ready(function () {
        $("#upload_img").HXAjaxUpload("AsyncUpLoadTest?f=1", "../Handlers/UploadProcessSync.ashx", function (per) {
            $("#per").css("width", per * 3 + "px");
            $("#per").html(per + "%");
        });
    });
</script>
